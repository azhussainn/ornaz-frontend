import * as React from 'react';
import { View, Text } from 'react-native';


function ForHerScreen ({ navigation }) {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
          <Text style={{ fontSize: 26 }}>
              For Her Screen
          </Text>
      </View>
    );
}

export default ForHerScreen 